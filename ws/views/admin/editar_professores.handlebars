<h3>Editar Professor:</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/professores/editar" method="POST">
        <input type="hidden" name="id" value="{{professor._id}}">
            <label for="ra">RA:</label>
            <input type="text" id="ra" name="ra" placeholder="RA do professor" class="form-control" value="{{professor.ra}}"> 

            <label for="nome_completo" class="mt-2">Nome completo:</label>
            <input type="text" id="nome_completo" name="nome_completo" placeholder="Nome do professor" class="form-control" value="{{professor.nome_completo}}">

            <label for="email" class="mt-2">Email:</label>
            <input type="email" id="email" name="email" placeholder="Email do professor" class="form-control" value="{{professor.email}}">
            <button type="submit" class="btn btn-success mt-3">Editar professor</button>
        </form>
    </div>
</div>